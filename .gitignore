node_modules
.nuxt
.output
dist
.env

# local database (seeded on startup)
.data

# full-resolution photography originals (kept outside public/ so Nuxt doesn't
# ship them; the optimised copies under public/images/ are committed)
product-originals/
lookbook-originals/

# local editors
.vscode
.idea
