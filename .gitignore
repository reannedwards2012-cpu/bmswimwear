node_modules
.nuxt
.output
dist
.env

# local database (seeded on startup)
.data

# full-resolution product photography originals (kept outside public/ so Nuxt
# doesn't ship them; the optimised copies under public/images/products/ are committed)
product-originals/

# local editors
.vscode
.idea
